# Native Mobile Module Paused

The Expo application in this directory is intentionally paused while the web application is developed as the primary installable PWA.

The native module remains available for a future phase. It is excluded from the root TypeScript project and is not part of the root web build.